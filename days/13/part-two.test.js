const partTwo = require('./part-two');

describe('day13 | part-two', () => {
  describe('#getResult', () => {
    it('should ', () => {
      const result = partTwo.getResult();

      expect(result).toBe();
    });
  });
});
