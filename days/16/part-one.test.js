const partOne = require('./part-one');

describe('day16 | part-one', () => {
  describe('#getResult', () => {
    it('should ', () => {
      const result = partOne.getResult();

      expect(result).toBe();
    });
  });
});
