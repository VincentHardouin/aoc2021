const partOne = require('./part-one');

describe('day11 | part-one', () => {
  describe('#getResult', () => {
    it('should ', () => {
      const result = partOne.getResult();

      expect(result).toBe();
    });
  });
});
