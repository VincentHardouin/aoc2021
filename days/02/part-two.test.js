const partTwo = require('./part-two');

describe('day2 | part-two', () => {
  describe('#getResult', () => {
    it('should return', () => {
      const result = partTwo.getResult();
    });
  });
});
