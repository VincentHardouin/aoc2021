const partOne = require('./part-one');

describe('day21 | part-one', () => {
  describe('#getResult', () => {
    it('should ', () => {
      const result = partOne.getResult();

      expect(result).toBe();
    });
  });
});
