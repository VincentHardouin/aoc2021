const partTwo = require('./part-two');

describe('day07 | part-two', () => {
  describe('#getResult', () => {
    it('should ', () => {
      const result = partTwo.getResult();

      expect(result).toBe();
    });
  });
});
