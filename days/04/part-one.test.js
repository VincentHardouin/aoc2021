const partOne = require('./part-one');

describe('day4 | part-one', () => {
  describe('#getResult', () => {
    it('should ', () => {
      const result = partOne.getResult();

      expect(result).toBe();
    });
  });
});
